@DAIRY_CONDITION_EV2

IF !RAND:10 && TALENT:MASTER:濃厚精液 && HAS_PENIS(MASTER)				;←ここに発生条件を入れよう　例として30分の1にしてるけど適当に変えてね
	CALL DAIRY_EV2
	RETURN 1
ELSE
	RETURN -1
ENDIF

@DAIRY_EV2
#DIMS MAS_HOME
PRINTFORML 半夜、%CALLNAME:MASTER%在幸福的感覺中醒了
PRINTFORMW 总感觉胯下温热的……
PRINTFORML ！！！！　糟了！是夢遺！
PRINTFORMW 如果能更有意義的射出去就好了…　%CALLNAME:MASTER%深感後悔
FLAG:71 = 0
TALENT:MASTER:120 = 0
BASE:MASTER:精力 -= 500
EXP:MASTER:射精経験 ++
IF RAND:10 == 0 && ITEM:奇跡之石 == 0
	PRINTFORML 气温、风向、湿度、大气压、曜日、月龄、行星排列等各种条件奇迹般组合的结果
	FONTBOLD
	PRINTFORM %CALLNAME:MASTER%射出的精液结晶化了
	CALL COLORMESSAGE("独占爱欲的奇迹之石", C_D_PINK, 0, 1)
	PRINTFORMW 生成了！
	FONTREGULAR
	CALL GET_ITEM(GETNUM(ITEM, "奇跡之石"), 1, 1)
	RETURN 1
ENDIF
CALL FISHER_YATES_SHAFFLE(CHARANUM)
LOCAL:1 = -1
MAS_HOME '= AREANAME_CHARA_HOME(MASTER)
FOR LOCAL, 0, CHARANUM
	;対象があなた
	SIF SHAFFLE_ARRAY:LOCAL == MASTER
		CONTINUE
	;でてこない
	SIF CFLAG:(SHAFFLE_ARRAY:LOCAL):出禁
		CONTINUE
	;同居してないよ
	;判定が多少緩いけど前よりはマシ
	SIF AREANAME_CHARA_HOME(SHAFFLE_ARRAY:LOCAL) != MAS_HOME
		CONTINUE
	LOCAL:1 = SHAFFLE_ARRAY:LOCAL
	BREAK
NEXT
SIF LOCAL:1 <= 0
	RETURN

PRINTFORML %CALLNAME:MASTER%洗着被夢遺弄髒的衣服時%CALLNAME:(LOCAL:1)%飛過來了！
PRINTFORMW %CALLNAME:(LOCAL:1)%不要把這種東西和其他要洗的衣服一起洗、從%CALLNAME:MASTER%的手中搶走了內衣…
JUEL:(LOCAL:1):11 += 1000
PRINTFORMW %CALLNAME:(LOCAL:1)%　欲情之珠＋1000
RETURN 1

