@DLCOPTIONPRINT
	PRINTFORML 
	PRINTFORML 
	PRINTFORML 设定内置补丁的启动与否
	PRINTFORML 点击选项可以查看补丁详细说明
	PRINTFORML 如果有BUG请按照补丁的说明文件在深渊&进群&邮件反馈
	SETCOLOR C_RED
	PRINTFORML 部分补丁可能会极大的降低游戏难度，请自己酌情启用！！！
	RESETCOLOR
	PRINTFORML 补丁适用游戏版本：4.735
	DRAWLINEFORM =
	PRINTFORML 现在的设定：
	PRINT 多恋人补丁：
	IF MORELOVEER == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 1] 多恋人补丁设置", 20%

	PRINT 天之声补丁：
	IF SKYSOUND == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 2] 天之声补丁设置", 20%

	PRINT 无限制泡澡：
	IF WASH == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 3] 无限制泡澡设置", 20%

	PRINT 防袭击补丁：
	IF ENCOUNTDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 4] 防袭击补丁设置", 20%

	PRINT 丰富的天气：
	IF WEATHERDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 5] 丰富的天气设置", 20%

	PRINT 幻想乡之主：
	IF DEBUGGERR == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 6] 幻想乡之主设置", 20%
	
	PRINT 重选欧金金：
	IF OUJINJIN == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 7] 重选欧金金设置", 20%

	PRINT 简单化实绩：
	IF BONUSX == 0 || BONUSX == 100
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 8] 简单化实绩设置", 20%

	PRINT 道歉免时间：
	IF !APLOGIZEDLC
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 9] 道歉免时间设置", 20%

	PRINT 无条件访问：
	IF !HOMEWEL
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[10] 无条件访问设置", 20%

	PRINT 工作可推倒：
	IF !JOBFUCKING
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[11] 工作可推倒设置", 20%

	PRINT 药物简易化：
	IF !DRUG
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[12] 药物简易化设置", 20%

	PRINT 属性简易化：
	IF ABLDLC == 0 || ABLDLC == 100
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[13] 属性简易化设置", 20%

	PRINT 弹幕战强化：
	IF BATTLEDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[14] 弹幕战强化设置", 20%

	PRINT 新妻泛用化：
	IF NEWWIFE == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[15] 新妻泛用化设置", 20%

	PRINT 胖次弹幕战：
	IF PANGCI == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[16] 胖次弹幕战设置", 20%

	PRINT 生动的记忆：
	IF MEETCHECKDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"[17] 生动的记忆设置", 20%
	
	;PRINT 更多的农业(未实装)：
	;IF FARMDLC == 0
	;	SETCOLOR 0x994C00
	;	PRINTFORM 未启动
	;ELSE
	;	SETCOLOR 0x00FF00
;		PRINTFORM 已启动
;	ENDIF
;	RESETCOLOR