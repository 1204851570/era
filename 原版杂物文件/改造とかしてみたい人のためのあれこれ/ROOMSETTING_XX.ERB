@ROOMSETTING_XX
VARSET ROOMDATA
FOR LOCAL,1,100
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_被窝
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_屋内
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_浴室
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_厨房
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_訓練
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_学习
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_休憩
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_廁所
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_露天
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_赏月
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_天之声
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_菜園
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_打水
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_开阔处
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_狭窄
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_打瞌睡_可能
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 場所_身体測定
NEXT

MAXROOM = 
ENTRANCE = 
避難处 = 
默认的初始位置 = 
拠点_大家 = 

;以下二つはよく変化するので個別に設定

@KICKOUT_X(ARG)
#FUNCTION

@BATHROOM_X()
#FUNCTION